class Products {
  constructor(names, price, screen, backCamera, frontCamera, img, desc, type) {
    this.names = names;
    this.price = price;
    this.screen = screen;
    this.backCamera = backCamera;
    this.frontCamera = frontCamera;
    this.img = img;
    this.desc = desc;
    this.type = type;
  }
}
